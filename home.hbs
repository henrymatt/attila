{{!< default}}
<header id="blog-header">
    <h1>Hi, I'm Matt.</h1>
    <img src="/assets/img/matt-portrait.jpg" alt="portrait of matt henry" class="blog-header__portrait"/>
    <p>I try to explain the things I like.</p>
</header>

<main role="main">
    <section class="topics">
        <div class="topics__list">
            <h2>The problem is, I like a lot of things.</h2>
            {{#get 'tags'}}
                {{#foreach tags}}
                    {{#if @last}}
                        <span class="topic"><a href='{{url}}'>{{name}}</a>.</span>
                    {{else}}
                        <span class="topic"><a href='{{url}}'>{{name}}</a>, </span>
                    {{/if}}
                {{/foreach}}
            {{/get}}
        </div>
    </section>

    <section class="article-previews">
	    <h2 class="article-previews__header">Latest articles</h2>
        {{#foreach posts}}
	        <a href="{{url}}">
	          <article class="article-preview">
		          <h2 class="article-preview--title">{{title}}</h2>
		          <div class="article-preview--topic">{{primary_tag.name}}</div>
		          <div class="article-preview--date">{{date}}</div>
	          </article>
	        </a>
        {{/foreach}}
    </section>
</main>


