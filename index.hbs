{{!< default}}
<div class="general-header">
	<h1>Articles</h1>
	<p>Maybe it’s educational, maybe it’s interesting, maybe neither.</p>
	<p>Here are the things I cared enough to write about.</p>
</div>
<main class="content" role="main"></main>
	<div class="tag-list">
		{{#get "tags" limit="all"}}
		{{#foreach tags}}
			<div class="tag-container">
				<div class="tag-name">
					<h2>{{name}}</h2>
				</div>
				<div class="tag-description">
					<p>{{description}}</p>
				</div>
				<div class="tag-link">
					<a href="{{url}}">All {{name}} Articles <span class="caret">▶</span></a>
				</div>
				{{#get "posts" filter="primary_tag:{{slug}}" limit="2"}}
				{{#foreach posts}}
				<article class="article-preview">
					<img src="{{feature_image}}" alt="">
					<p>{{title}}</p>
				</article>
				{{/foreach}}
				{{/get}}
			</div>
		{{/foreach}}
	{{/get}}
	</div>
</main>